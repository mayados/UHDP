{% extends 'base.html.twig' %}

{% block title %}Visualisation utilisateur{% endblock %}

{% block body %}

<div class="container-page moderation">

    {% include '_partials/_moderateurnav.html.twig' %}
    <main class="main-administration">
        <h1>{{ user.pseudo }}</h1>

        <figure class="figure-element">
            {% if not user.photo %}
                <img src="{{ asset('img/default_user.png') }}" alt="">
            {% else %}
                <img src="{{ asset('uploads/imgUserProfil/'~ user.photo) }}" alt="">   
            {% endif %}     
        </figure>
        {% if user.bannir == true %}
            <a class="manage-button" href="{{ path('ban_moderateur_user', {'id': user.id}) }}">Dé-bannir</a>
        {% else %}
            <a class="manage-button" href="{{ path('ban_moderateur_user', {'id': user.id}) }}">Bannir</a>
        {% endif %}  
    

    </main>        

</div>
{% endblock %}
