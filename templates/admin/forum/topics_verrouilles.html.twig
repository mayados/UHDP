{% extends 'base.html.twig' %}

{% block title %}Topics verrouilés{% endblock %}

{% block body %}

<div class="container-page administration">

        {% include '_partials/_adminnav.html.twig' %}
        <main class="main-administration">
            <h1>Topics verrouilés</h1>
            {% for topic in topicsVerrouilles %}
                <div>
                    <p>{{ topic.titre }} {{ topic.auteur}}</p>    
                    <a href="#">Modérer</a> 
                </div>
            {% endfor %}
            <div class="pagination js-filter-pagination">
                {{ knp_pagination_render(topicsVerrouilles) }}
            </div>            
        </main>        

</div>
{% endblock %}