{# <h2>Condoléances</h2>
{% if app.user %}
    <h3>Ajouter un message de condoléances</h3>
    {% include '_partials/_refreshForm.html.twig' %}  
{% endif %} #}
<div id="container-condoleances" class="js-auto-refresh-content">
        {% if condoleances| length > 0 %}
                {% for condoleance in condoleances %}
                        {# Laisser cette partie en visible de base puis la cachée lorsque le bouton est cliqué #}
                        <div class="condoleance-memorial" id="modify-part">
                                <div class="condoleance-body">
                                        <div class="condoleance-infos">
                                                <p>{{ condoleance.auteur }}</p>   
                                                <p>{{ condoleance.dateCreation| date('d-m-Y') }}</p>                                                 
                                        </div>
                                        <div class="condoleance-texte">
                                                <p>{{ condoleance.texte| raw }}</p>                                
                                        </div>                                                                   
                                </div>

                                <div class="condoleance-action">
                                        {% if app.user == condoleance.auteur %}
                                                {% if consultedInCategorie == true %}
                                                        <a href="{{path('remove_condoleance_categorie', {'idMemorial': memorial.id, 'id': condoleance.id, 'idCategorie': memorial.categorieAnimal.id}) }}"><i class="fa-solid fa-trash"></i></a>
                                                        <button class="modify-button">Modifier</button>   
                                                {% else %}
                                                        <a href="{{path('remove_condoleance', {'idMemorial': memorial.id, 'id': condoleance.id}) }}"><i class="fa-solid fa-trash"></i></a>
                                                        <button class="modify-button">Modifier</button>   
                                                {% endif %}
                                        {% endif %}                                
                                </div>
                        </div>
                        <div class="modify-form">
                                <form method="POST" action="{{path('edit_condoleance',{'id':condoleance.id, 'idMemorial':memorial.id})}}">
                                        <div class="form-element">
                                                <label for="inputPassword">Texte</label>
                                                <textarea name="texte" class="form-control">{{ condoleance.texte| raw }}</textarea>  
                                                <button type="submit" name="submit" value="submit" class="green">Modifier</button>         
                                        </div>
                                </form>                                
                        </div>
                {% endfor %}   
        {% endif %}
        <div class="condoleance-memorial-equipe">
                <div>
                        <p>{{ memorial.dateCreation| date('d-m-Y') }}</p>                          
                </div>
                <div>
                        <p>L'équipe d'Une Histoire de Pattes se joint à vous dans vos peines et vous adresse de sincères condoléances.</p>      
                </div>
        </div>        
</div>   
