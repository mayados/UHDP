<section class="memoriaux js-filter-content">
    <div class="container-memoriaux">
        {% for memorial in memoriaux %}
            {% include '_partials/_memorialCard.html.twig' with {memorial:memorial} only %}
        {% endfor %}    
    </div>
</section>