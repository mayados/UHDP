{{ form_start(formCondoleance, {attr: {class: 'js-submit-form'}}) }}    
    {{ form_widget(formCondoleance.texte, {'attr': {'id': 'condoText'}}) }}    
    {{ form_row(formCondoleance.submit) }}    
{{ form_end(formCondoleance) }}

