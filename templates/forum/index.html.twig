{% extends 'base.html.twig' %}

{% block title %}Forum{% endblock %}

{% block body %}

<div class="container-page">
    <h2>Les topics</h2>

    {% for topic in topics %}
    <div class="topic">
        {{ topic.titre }}
        <a href="{{path('show_topic', {'id': topic.id}) }}">Voir le topic</a>
    </div>
    {% endfor %}

</div>
{% endblock %}
