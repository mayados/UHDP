{% extends 'base.html.twig' %}

{% block title %}Hello MotCommemorationController!{% endblock %}

{% block body %}

<div class="container-page">
    <h3>Mur de commémoration</h3>

    {% for mot in mots %}
        <div class="mot">
            <p>{{ mot.mot }}
            ({{ mot.auteur }}
            {{ mot.dateCreation | date('d-m-Y') }})
            </p>
        </div>
    {% endfor %}

    {{ form_start(formAddMot) }}
    {{ form_end(formAddMot) }}
</div>
{% endblock %}
